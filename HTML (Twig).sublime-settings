// These settings override both User and Default settings for the HTML (Twig) syntax
{
	"auto_complete_selector": "meta.expression.twig, meta.statement.twig, meta.tag, source - comment - string.quoted.double.block - string.quoted.single.block - string.unquoted.heredoc"
}
